from flask_definitions import *

# todo for the ADMIN Endpoints make POST requests so the streamlit site can change those things.
# Admin endpoints will be verified via a Cookie and if that does not exist forwarded to /login.
# This has to be done in streamlit so here will be a login endpoints, get data and update data.
# BASE /api/v1/
# Endpoints: Login, Register (Maybe only let DEVS create accounts so nothing gets flooded?)
# Endpoints with POST as Update and GET as get: game, game_info, patch
# For the User this Data will be compiled into one view which has all of the needed data and they can change anything.

# How to Handle game creation? Let the user add them and check by dev or?

# Add Mod support? So Users can make theyr own mods like visuals and add them to the game?
